---
sidebar_position: 1
sidebar_label: "Test Page"
---
{/* content/course/javascript/welcome.mdx */}

import PageInitializer from "@site/src/modules/quiz/components/PageInitializer";
import ResetButton from "@site/src/modules/quiz/components/ResetButton";
import SubmitButton from "@site/src/modules/quiz/components/SubmitButton";
import TestComponent from "@site/src/components/TestComponent";
import TrueFalseQuestion from "@site/src/modules/quiz/components/TrueFalseQuestion";
import MultipleChoiceQuestion from "@site/src/modules/quiz/components/MultipleChoiceQuestion";
import CodeChoiceQuestion from "@site/src/modules/quiz/components/CodeChoiceQuestion";
import ShortAnswerQuestion from "@site/src/modules/quiz/components/ShortAnswerQuestion";
import CodeEditor from "@site/src/modules/editor/components/CodeEditor";
import Accordion1 from "@site/src/components/Accordion";
import { UserRole } from "@site/src/modules/user/types/user.type";
import { Blue } from "@site/src/components/Highlights";

# Welcome (Test Page)

<PageInitializer
    pageData={{
        trackId: 1,
        pageId: 1,
        // Metadata
        type: "quiz",
        course: "Welcome",
        module: "Introduction",
        section: "Introduction",
        title: "Test Page",
        topic: "Topic 1",
        description: "This is the welcome.mdx test page.",
        tags: ["test", "welcome", "intro"],
        // Images
        image: "images/levels/junior-developer.png",
        banner: "images/levels/junior-developer.png",
        // Data
        order: 1,
        coursePathProgress: 1, // Keep the fields relevant to the progress tracking and quizzes
        difficulty: 3,
        prerequisites: [],
        points: 10,
        // Access
        requiresAuth: false, // FIXME: Test requiresAuth true for this page
        roles: [UserRole.Admin],
    }}
/>


### User Profile Summary

Given a JavaScript object containing basic profile data (e.g., name, email, etc.), iterate over its properties and display a formatted summary.

A `user` <Blue>**object**</Blue> containing user profile data.

```JavaScript
const user = {
    name: "John Doe",
    email: "jdoe@me.com",
    phone: "555-555-5555",
}
```

<TrueFalseQuestion
    questionId={1}
    order={1}
    difficulty={2}
    question="JavaScript is a strongly-typed language."
    correctAnswer={false}
    points={10}
/>
<br />

<Accordion1 title="Explanation">
    Reveal? JavaScript is a weakly-typed language, meaning that variables cannot
    change __. Low-level languages like C++ and Rust have greater control over
    the system hardware, like memory and CPU, and so... **Dog**
</Accordion1>

<br />

## Parsing Integers

```js
const date = Number.parseInt("21 February 2024");
```

<MultipleChoiceQuestion
    questionId={2}
    type="multipleChoice"
    difficulty={3}
    order={2}
    points={10}
    question={`What is the value of \`date\`?`}
    options={["Null", "21", "NaN", "212024"]}
    correctAnswer={1}
/>

<br />

```js
const array = [1, 2, 3, 4, 5, 6];
const output = 6 in array;
```

<ShortAnswerQuestion
  questionId={3}
  type="shortAnswer"
  difficulty={3}
  order={3}
  points={10}
  question={`What is the correct value of \`output\`?`}
  correctAnswer={"5"} 
/>

<br />


```js
const a = 5;
const b = '5';
console.log(a != b)
```
```js
const a = 5;
const b = '5';
console.log(a == b)
```
```js
const a = 5;
const b = '5';
console.log(a === b)
```

<CodeChoiceQuestion
questionId={4}
correctAnswer={2}
points={10}
question="Choose the correct block of code."
/>

<br />
<TestComponent />
<br />

<CodeEditor
    height={300}
    minimap={false}
    initialCode={`function sum(a, b) {\n    // Your code here\n}`}
    testCases={[
        { input: [1, 2], expected: 3 },
        { input: [5, 5], expected: 10 },
    ]}
    testCode={`function runTest(userCode, testCase) {
        const { input, expected } = testCase;
        try {
            const userFunction = new Function('return ' + userCode)();
            const result = userFunction(...input);
            return result === expected ? 'Pass' : 'Fail';
        } catch (error) {
            return 'Error';
        }
    }`}
/>

<ResetButton />
<SubmitButton />